package org.StarWarsFinalProject;

/**
 * enum class which includes all of the entity types for each class
 */
public enum EntityType {
    PLAYER,
    OPPONENT,
    WEAPON,
    Blaster,
    Bullets,
    PLAYERWEAPON,
    OPPONENTWEAPON
}